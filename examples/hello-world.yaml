# Simple hello world workflow
# Run with: kaigi run examples/hello-world.yaml

name: hello-world
version: "1.0"
description: A simple workflow that demonstrates basic agent chaining
steps:
  - id: greet
    command: echo
    args: ["Hello from Kaigi!"]

  - id: timestamp
    command: date
    args: ["+%Y-%m-%d %H:%M:%S"]
